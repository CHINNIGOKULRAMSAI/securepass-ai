# DAA Implementation — Password Analyzer

Frontend (development):

```bash
cd frontend
npm install
npm run dev
```

Backend: see backend/ for Python FastAPI service. Start backend before using the frontend so analysis requests succeed.

This repo contains a simple React + Vite frontend that talks to the backend at `http://127.0.0.1:8000/analyze` by default.
